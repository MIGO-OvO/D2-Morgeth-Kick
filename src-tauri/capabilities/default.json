{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main calibration window and read-only overlay access",
  "windows": ["main", "overlay"],
  "permissions": ["core:default"]
}
